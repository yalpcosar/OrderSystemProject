# Angular Architecture Overview - DevArchitecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Angular Application                      │
│                      (Angular 16.2.12)                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       App Module                             │
│  • BrowserAnimationsModule                                   │
│  • HttpClientModule                                          │
│  • RouterModule                                              │
│  • TranslateModule                                           │
│  • Material Modules                                          │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────────┐    ┌──────────────┐
│  Interceptor │    │   Login Guard    │    │   Services   │
│  (JWT Auth)  │    │  (Route Guard)   │    │  (Shared)    │
└──────────────┘    └──────────────────┘    └──────────────┘
```

---

## 📂 Folder Structure Visualization

```
src/app/
│
├── core/                                   [CORE INFRASTRUCTURE]
│   │
│   ├── components/                         [UI COMPONENTS]
│   │   ├── admin/                          [ADMIN MODULES]
│   │   │   ├── ✅ group/                   [EXISTING]
│   │   │   ├── ✅ language/                [EXISTING]
│   │   │   ├── ✅ log/                     [EXISTING]
│   │   │   ├── ✅ login/                   [EXISTING]
│   │   │   ├── ✅ operationclaim/          [EXISTING]
│   │   │   ├── ✅ translate/               [EXISTING]
│   │   │   ├── ✅ user/                    [EXISTING]
│   │   │   │
│   │   │   ├── 🔴 pcolor/                  [TO BE CREATED]
│   │   │   │   ├── models/
│   │   │   │   │   └── PColor.ts
│   │   │   │   ├── services/
│   │   │   │   │   ├── PColor.service.ts
│   │   │   │   │   └── PColor.service.spec.ts
│   │   │   │   ├── pcolor.component.ts
│   │   │   │   ├── pcolor.component.html
│   │   │   │   ├── pcolor.component.scss
│   │   │   │   └── pcolor.component.spec.ts
│   │   │   │
│   │   │   ├── 🔴 customer/                [TO BE CREATED]
│   │   │   ├── 🔴 product/                 [TO BE CREATED]
│   │   │   ├── 🔴 order/                   [TO BE CREATED]
│   │   │   └── 🔴 warehouse/               [TO BE CREATED]
│   │   │
│   │   └── app/                            [APP LAYOUT]
│   │       ├── ✅ dashboard/
│   │       ├── ✅ footer/
│   │       ├── ✅ layouts/
│   │       ├── ✅ navbar/
│   │       └── ✅ sidebar/
│   │
│   ├── constants/                          [CONSTANTS]
│   │   └── ✅ api-url.ts
│   │
│   ├── directives/                         [CUSTOM DIRECTIVES]
│   │   └── ✅ must-match.ts
│   │
│   ├── guards/                             [ROUTE GUARDS]
│   │   └── ✅ login-guard.ts
│   │
│   ├── interceptors/                       [HTTP INTERCEPTORS]
│   │   └── ✅ auth-interceptor.service.ts
│   │
│   ├── models/                             [SHARED MODELS]
│   │   └── ✅ LookUp.ts
│   │
│   ├── modules/                            [FEATURE MODULES]
│   │   ├── ✅ admin-layout.module.ts       [⚠️ NEEDS UPDATE]
│   │   └── ✅ components.module.ts
│   │
│   └── services/                           [SHARED SERVICES]
│       ├── ✅ Alertify.service.ts
│       ├── ✅ http-entity-repository.service.ts
│       ├── ✅ local-storage.service.ts
│       ├── ✅ LookUp.service.ts            [⚠️ NEEDS EXTENSION]
│       ├── ✅ shared.service.ts
│       └── ✅ Translation.service.ts
│
├── ✅ app.component.ts
├── ✅ app.module.ts
└── ✅ app.routing.ts
```

**Legend:**
- ✅ **Existing** - Already implemented, can be reused
- ⚠️ **Needs Update** - Existing but requires modification
- 🔴 **To Be Created** - New module to be developed

---

## 🔄 Data Flow Architecture

```
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │
       │ 1. HTTP Request
       ▼
┌────────────────────────┐
│  Auth Interceptor      │◄───── Injects JWT Token
│  (auth-interceptor.ts) │       Adds Accept-Language
└──────┬─────────────────┘
       │
       │ 2. Authenticated Request
       ▼
┌──────────────────────────┐
│    Backend API           │
│  (https://localhost:5001)│
└──────┬───────────────────┘
       │
       │ 3. Response
       ▼
┌────────────────────────┐
│  Component Service     │
│  (e.g., User.service)  │
└──────┬─────────────────┘
       │
       │ 4. Observable Data
       ▼
┌────────────────────────┐
│   Component            │
│  (e.g., UserComponent) │
└──────┬─────────────────┘
       │
       │ 5. Data Binding
       ▼
┌────────────────────────┐
│  Material Table        │
│  + Pagination/Sort     │
└────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌────────────┐
│   Login    │
│  Component │
└─────┬──────┘
      │
      │ 1. Submit Credentials
      ▼
┌─────────────┐
│ Auth Service│
└─────┬───────┘
      │
      │ 2. POST /Auth/login
      ▼
┌──────────────┐
│  Backend API │
└─────┬────────┘
      │
      │ 3. Return JWT + Refresh Token
      ▼
┌──────────────────┐
│ LocalStorage     │
│  • token         │
│  • refreshToken  │
│  • lang          │
└─────┬────────────┘
      │
      │ 4. Decode JWT
      ▼
┌──────────────────┐
│ Auth Service     │
│  • userName      │
│  • claims[]      │
└─────┬────────────┘
      │
      │ 5. Navigate to Dashboard
      ▼
┌──────────────────┐
│  Dashboard       │
└──────────────────┘

┌──────────────────────────┐
│  Subsequent Requests     │
└──────────┬───────────────┘
           │
           │ 1. Intercept Request
           ▼
┌──────────────────────────┐
│  Auth Interceptor        │
│  • Add Authorization     │
│  • Add Accept-Language   │
└──────────┬───────────────┘
           │
           │ 2. If 401 Error
           ▼
┌──────────────────────────┐
│  Token Service           │
│  • POST /Auth/refresh    │
│  • Get new tokens        │
│  • Retry original request│
└──────────────────────────┘
```

---

## 🎨 Component Architecture Pattern

```
┌──────────────────────────────────────────────────────┐
│                    Component                          │
│  ┌────────────────────────────────────────────────┐  │
│  │          TypeScript Component Class             │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Properties                               │  │  │
│  │  │  • dataSource: MatTableDataSource        │  │  │
│  │  │  • list: Model[]                         │  │  │
│  │  │  • form: FormGroup                       │  │  │
│  │  │  • dropdownSettings                      │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Lifecycle Hooks                         │  │  │
│  │  │  • ngOnInit()                            │  │  │
│  │  │  • ngAfterViewInit()                     │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  CRUD Methods                            │  │  │
│  │  │  • getList()                             │  │  │
│  │  │  • getById(id)                           │  │  │
│  │  │  • save()                                │  │  │
│  │  │  • add()                                 │  │  │
│  │  │  • update()                              │  │  │
│  │  │  • delete(id)                            │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │                                                 │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Utility Methods                         │  │  │
│  │  │  • clearFormGroup()                      │  │  │
│  │  │  • checkClaim()                          │  │  │
│  │  │  • configDataTable()                     │  │  │
│  │  │  • applyFilter()                         │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │               HTML Template                     │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Search/Filter Section                   │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Material Table                          │  │  │
│  │  │  • mat-table                             │  │  │
│  │  │  • mat-paginator                         │  │  │
│  │  │  • mat-sort                              │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │  Modal Dialog (Bootstrap)                │  │  │
│  │  │  • Form Fields                           │  │  │
│  │  │  • Dropdowns                             │  │  │
│  │  │  • Save/Cancel Buttons                   │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │                  Service                        │  │
│  │  • getList()                                    │  │
│  │  • getById(id)                                  │  │
│  │  • add(item)                                    │  │
│  │  • update(item)                                 │  │
│  │  • delete(id)                                   │  │
│  └────────────────────────────────────────────────┘  │
│                           │                           │
└───────────────────────────┼───────────────────────────┘
                            │
                            ▼
                ┌───────────────────────┐
                │     Backend API        │
                │  (ASP.NET Core)        │
                └───────────────────────┘
```

---

## 🔗 Module Dependencies

```
┌─────────────┐
│   Colors    │  (Independent)
│  (PColor)   │
└──────┬──────┘
       │
       │ Provides lookup for
       ▼
┌─────────────┐
│  Products   │  (Depends on Colors)
└──────┬──────┘
       │
       ├─────────────────────┐
       │                     │
       │                     ▼
       │              ┌─────────────┐
       │              │  Warehouse  │  (Depends on Products)
       │              └─────────────┘
       │
       │ Provides lookup for
       ▼
┌─────────────┐
│   Orders    │  (Depends on Products & Customers)
└──────┬──────┘
       │
       │ Also depends on
       ▼
┌─────────────┐
│  Customers  │  (Independent)
└─────────────┘
```

**Development Order:**
1. Colors (PColor) - No dependencies
2. Customers - No dependencies
3. Products - Depends on Colors
4. Warehouse - Depends on Products
5. Orders - Depends on Products & Customers

---

## 🛡️ Security Architecture

```
┌──────────────────────────────────────────────────────────┐
│                     Authorization                         │
└──────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ Route Level  │   │  Component   │   │  UI Element  │
│              │   │    Level     │   │    Level     │
└──────────────┘   └──────────────┘   └──────────────┘
        │                   │                   │
        │                   │                   │
        ▼                   ▼                   ▼

LoginGuard          checkClaim()        *ngIf="checkClaim()"

canActivate: [      return this.        <button *ngIf=
  LoginGuard        authService.         "checkClaim(
]                   claimGuard(           'UpdateCmd'
                     'GetQuery'          )">
                    )                   </button>
```

### Claims Pattern
Each module has 4 main claims:
- `GetModulesQuery` - View/list
- `CreateModuleCommand` - Create
- `UpdateModuleCommand` - Update
- `DeleteModuleCommand` - Delete

---

## 🌐 Internationalization Architecture

```
┌─────────────────────┐
│  Translation Keys   │
│  (Database/API)     │
└──────────┬──────────┘
           │
           │ GET /translates/languages/{lang}
           ▼
┌─────────────────────┐
│ TranslationService  │
│  (Custom Loader)    │
└──────────┬──────────┘
           │
           │ Implements TranslateLoader
           ▼
┌─────────────────────┐
│ @ngx-translate/core │
└──────────┬──────────┘
           │
           │ Provides translations to
           ▼
┌─────────────────────┐
│    Components       │
│  (via pipe/service) │
└─────────────────────┘

Usage in Components:
━━━━━━━━━━━━━━━━━━━━━
Template:  {{ 'KEY' | translate }}
TypeScript: this.translateService.get('KEY')
Service:   this.alertifyService.success('KEY')
```

**Supported Languages:**
- `en-US` - English
- `tr-TR` - Turkish

**Storage:** LocalStorage key: `lang`

---

## 📊 State Management

```
┌──────────────────────────────────────────────────────┐
│                  Component State                      │
│  ┌────────────────────────────────────────────────┐  │
│  │  Local State (Component)                       │  │
│  │  • list: Model[]                               │  │
│  │  • dataSource: MatTableDataSource              │  │
│  │  • form: FormGroup                             │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │  Shared State (Services)                       │  │
│  │  • AuthService (user, claims)                  │  │
│  │  • LocalStorageService (token, lang)           │  │
│  │  • SharedService (events via Subject)          │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │  Persistent State (LocalStorage)               │  │
│  │  • token                                       │  │
│  │  • refreshToken                                │  │
│  │  • lang                                        │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

**Note:** No NgRx or state management library is used. State is managed locally in components and shared via services.

---

## 🎨 UI Component Hierarchy

```
┌────────────────────────────────────────────────────────┐
│               AdminLayoutComponent                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │               NavbarComponent                     │  │
│  │  • User Info                                      │  │
│  │  • Logout                                         │  │
│  │  • Help Link                                      │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────┐  ┌───────────────────────────────────┐  │
│  │          │  │                                    │  │
│  │ Sidebar  │  │       <router-outlet>             │  │
│  │          │  │                                    │  │
│  │ • Admin  │  │  ┌──────────────────────────────┐ │  │
│  │   Routes │  │  │    Page Component            │ │  │
│  │          │  │  │  • DashboardComponent        │ │  │
│  │ • User   │  │  │  • UserComponent             │ │  │
│  │   Routes │  │  │  • GroupComponent            │ │  │
│  │          │  │  │  • PColorComponent  🔴       │ │  │
│  │ • Claims │  │  │  • CustomerComponent 🔴      │ │  │
│  │   Filter │  │  │  • ProductComponent 🔴       │ │  │
│  │          │  │  │  • OrderComponent 🔴         │ │  │
│  │          │  │  │  • WarehouseComponent 🔴     │ │  │
│  │          │  │  └──────────────────────────────┘ │  │
│  │          │  │                                    │  │
│  └──────────┘  └───────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │               FooterComponent                     │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

---

## 📦 Material Design Components Used

```
┌─────────────────────────────────────────────────────┐
│          Angular Material Modules                   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ✅ MatButtonModule         • Buttons              │
│  ✅ MatInputModule          • Input fields         │
│  ✅ MatFormFieldModule      • Form field wrapper   │
│  ✅ MatSelectModule         • Dropdowns            │
│  ✅ MatTableModule          • Data tables          │
│  ✅ MatPaginatorModule      • Table pagination     │
│  ✅ MatSortModule           • Table sorting        │
│  ✅ MatCheckboxModule       • Checkboxes           │
│  ✅ MatTooltipModule        • Tooltips             │
│  ✅ MatRippleModule         • Ripple effects       │
│                                                     │
│  ✅ ng-multiselect-dropdown • Multi-select         │
│  ✅ Bootstrap Modals        • Dialog boxes         │
│  ✅ SweetAlert2             • Fancy alerts         │
│  ✅ Alertify                • Notifications        │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🚀 Build & Deployment Pipeline

```
┌──────────────┐
│  TypeScript  │
│   Source     │
└──────┬───────┘
       │
       │ ng build
       ▼
┌──────────────┐
│   Angular    │
│   Compiler   │
└──────┬───────┘
       │
       │ AOT Compilation
       ▼
┌──────────────┐
│  JavaScript  │
│   Bundles    │
└──────┬───────┘
       │
       │ Optimization
       ▼
┌──────────────┐
│  Production  │
│    Build     │
│   (dist/)    │
└──────────────┘

Development:  ng serve (port 4200)
Production:   ng build --configuration production
```

---

## 📈 Performance Considerations

```
┌────────────────────────────────────────────────┐
│          Performance Optimizations              │
├────────────────────────────────────────────────┤
│                                                 │
│  ✅ Lazy Loading         • Feature modules     │
│                           loaded on demand     │
│                                                 │
│  ✅ AOT Compilation      • Faster rendering    │
│                           Smaller bundle size  │
│                                                 │
│  ✅ OnPush Detection     • Not currently used  │
│     (Opportunity)         Could improve perf   │
│                                                 │
│  ✅ Virtual Scrolling    • Not currently used  │
│     (Opportunity)         For large lists      │
│                                                 │
│  ✅ Track By Functions   • Should be added     │
│     (Opportunity)         to *ngFor loops      │
│                                                 │
└────────────────────────────────────────────────┘
```

---

## 🧪 Testing Strategy

```
┌────────────────────────────────────────────────┐
│              Testing Layers                     │
├────────────────────────────────────────────────┤
│                                                 │
│  Unit Tests         • *.spec.ts files          │
│  (Jasmine/Karma)    • Service testing          │
│                     • Component testing        │
│                                                 │
│  E2E Tests          • Protractor config        │
│  (Protractor)       • User flow testing        │
│                     • Integration testing      │
│                                                 │
│  Manual Testing     • Browser testing          │
│                     • Claims verification      │
│                     • Responsive design        │
│                                                 │
└────────────────────────────────────────────────┘
```

---

## 📝 Naming Conventions

```
┌────────────────────────────────────────────────┐
│           File Naming Patterns                  │
├────────────────────────────────────────────────┤
│                                                 │
│  Components:    module-name.component.ts        │
│  Services:      ModuleName.service.ts           │
│  Models:        ModuleName.ts                   │
│  Guards:        name-guard.ts                   │
│  Interceptors:  name-interceptor.service.ts     │
│  Directives:    directive-name.ts               │
│  Pipes:         pipe-name.pipe.ts               │
│                                                 │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│           Class Naming Patterns                 │
├────────────────────────────────────────────────┤
│                                                 │
│  Components:    ModuleNameComponent             │
│  Services:      ModuleNameService               │
│  Guards:        NameGuard                       │
│  Interceptors:  NameInterceptorService          │
│  Models:        ModuleName                      │
│                                                 │
└────────────────────────────────────────────────┘
```

---

*Last Updated: October 10, 2025*
*Angular Version: 16.2.12*
*Material Design: 16.2.14*

